(
    axes: {
        "strafe_vertical": Emulated(pos: Key(LShift), neg: Key(Space)),
	"strafe_horizontal": Emulated(pos: Key(D), neg: Key(A)),
	"strafe_normal": Emulated(pos: Key(S), neg: Key(W)),
	"roll": Emulated(pos: Key(E), neg: Key(Q))
    },
    actions: {
        "exit": [[Key(Escape)]],
    },
)